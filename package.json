{
  "name": "membot",
  "version": "1.0.0",
  "description": "Molong: a Walrus Memory-backed chatbot with cross-session recall",
  "main": "membot.mjs",
  "scripts": {
    "start": "node membot.mjs",
    "test": "node --test tests/*.test.mjs",
    "check": "node --check membot.mjs && node --check rate_limit.mjs && node --check memory_note.mjs && node --check verify_mainnet.mjs && node --check verify_memory.mjs && node --check count_namespaces.mjs",
    "verify:mock": "USE_MOCK=1 node verify_memory.mjs",
    "verify:mainnet": "node verify_mainnet.mjs",
    "count:blobs": "node count_namespaces.mjs"
  },
  "engines": {
    "node": ">=20"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "dependencies": {
    "@mysten-incubation/memwal": "^0.1.7",
    "@mysten/seal": "^1.1.0",
    "@mysten/sui": "^2.5.0",
    "@mysten/walrus": "^1.0.3",
    "express": "^5.2.1"
  }
}
